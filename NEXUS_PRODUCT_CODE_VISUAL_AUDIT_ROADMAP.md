# NEXUS AI — مراجعة الكود، الأوديت البصري، وخريطة الطريق

تاريخ المراجعة: 30 يوليو 2026

## حالة التنفيذ — ثماني دفعات

تم تنفيذ أول دفعة من خريطة الطريق في نفس تاريخ المراجعة:

- توحيد الجلسات على Supabase SSR Cookies والتحقق من JWT في middleware بدل الثقة في وجود Cookie.
- حماية كل صفحات مساحة العمل بسياسة واحدة، مع إبقاء صفحات العملاء والمراجعين العامة متاحة.
- ترحيل صامت لمستخدمي `localStorage` الحاليين دون إجبارهم على تسجيل الدخول من جديد.
- إضافة مهلة زمنية وRetry للأعطال المؤقتة و429 و5xx، وأخطاء منظمة لطلبات AI الأساسية.
- إضافة Circuit Breaker يوقف الطلبات المكلفة مؤقتًا بعد الأعطال المتكررة، مع Half-open recovery تلقائي.
- إضافة Health Check فعلي ينفذ توليدًا مصغرًا ويتحقق من المفتاح والموديل، ويصنف Auth/Quota/Rate-limit/Network دون كشف الأسرار، مع Cache يحمي الرصيد.
- توحيد اللغة وإزالة Provider المكرر، مع إخراج `lang` و`dir` الصحيحين من السيرفر قبل أول رسم.
- تبسيط القائمة الرئيسية لصاحب الشركة إلى: Today، My decisions، Results، Settings & connections، ووضع أدوات المتخصص داخل Advanced tools.
- تبسيط أول بطاقة في Today لتوضح ما يجهزه NEXUS وما يحتاج قرار المستخدم، دون تقديم نسبة الجاهزية كأنها نتيجة أداء.
- إزالة `eslint.ignoreDuringBuilds` حتى يصبح فحص Lint وTypeScript جزءًا من Production Build.

### الدفعة الثانية — Automation دائمة لمحرك الحملات

تم تنفيذ أول مسار طويل قابل للاستئناف بدل إبقاء المستخدم داخل طلب HTTP واحد:

- إضافة `AutomationJob` و`AutomationJobStep` كطابور Postgres منخفض التكلفة، مع سجل المحاولات والخطوات والتقدم والأخطاء.
- منع تشغيل مهمتين نشطتين على الحملة نفسها، ومنع تكرار العملية نفسها بمفاتيح Idempotency على مستوى مساحة العمل.
- Claim ذري للمهام باستخدام `FOR UPDATE SKIP LOCKED`، وLease أطول من أقصى مدة للعامل لمنع التنفيذ المتوازي المكرر.
- Retry تلقائي بأوقات متزايدة للأعطال المؤقتة في المزود وقاعدة البيانات، مع استرداد الرصيد المحجوز عند الفشل النهائي.
- تشغيل فوري بعد الاستجابة عبر `after()`، مع Cron كل خمس دقائق كشبكة أمان للمهام المتوقفة أو المؤجلة.
- تحويل Campaign Engine إلى `202 Accepted` عندما تطلب الواجهة التنفيذ غير المتزامن، مع استمرار المسار المتزامن للتوافق مع العملاء الحاليين.
- إضافة API مملوك للمستخدم فقط لقراءة حالة المهمة وإعادة إيقاظها، دون كشف المدخلات الخاصة أو مفاتيح Idempotency أو Lease.
- استعادة متابعة المهمة تلقائيًا بعد تحديث صفحة الحملة أو الرجوع إليها، وعرض التقدم وإعادة المحاولة بلغة أعمال بسيطة.
- إدخال جداول الـAutomation في Health Readiness وWorkspace Reset وعقد migrations.
- تصحيح قياس تكلفة الاستراتيجية ليستخدم تعريفة `gpt-4o` الفعلية.

هذه الدفعة بنت **أساس Orchestrator فعلي لأول Workflow**؛ الدفعة التالية وسّعت الأساس إلى حزمة المحتوى والميديا والموافقة، لكنها لا تعني أن Autopilot الكامل من الاكتشاف حتى النشر والتعلّم أصبح منجزًا.

### الدفعة الثالثة — حزمة المحتوى واتجاهات الميديا والموافقة

تم تحويل أهم انتقال بعد اعتماد الاستراتيجية من طلب طويل متزامن إلى Workflow دائم:

- أصبح إنشاء Content Plan يدعم `202 Accepted` مع نفس فحوص الملكية، اعتماد الاستراتيجية، جودة Brand Brain، نطاق القنوات، حدود الخطة، وتوفر مزود AI قبل حجز الرصيد.
- حجز الرصيد يحدث مرة واحدة قبل إدخال المهمة إلى الطابور، والعامل يعيد استخدام نفس Transaction بدل تخزين Session أو خصم جديد.
- المهمة الجديدة `CAMPAIGN_APPROVAL_PACKAGE` تنتج النصوص واتجاهات الصور والفيديو، وتوزع ميديا مساحة العمل تلقائيًا عندما لا يحدد المستخدم ملفات بعينها.
- لا يتم توليد صور أو فيديو مدفوع ضمن هذا المسار بصمت؛ الناتج منخفض التكلفة هو Copy + Media Direction + Owned-media assignment.
- تتوقف المهمة عند `WAITING_FOR_APPROVAL` مع رابط مراجعة واحد وملخص آمن، وتثبت صراحة أن `publishAuthorized=false` و`spendAuthorized=false`.
- تحديث الصفحة أو مغادرة غرفة الحملة لا يبدأ طلب AI أو خصمًا جديدًا؛ الواجهة تستعيد المهمة وتعرض التقدم، ثم تعرض زر «راجع الحزمة ووافق».
- اعتماد Content Plan يحول المهمة إلى `COMPLETED` داخل نفس Transaction التي تحفظ Snapshot وسجل القرار. حذف المسودات يحولها إلى `CANCELLED`.
- Recovery بعد انتهاء Lease يكتشف المسودات التي حُفظت بعد إنشاء المهمة ويكمل تسوية الرصيد والحالة دون إعادة توليد المحتوى.
- تمت إضافة فهرس مركب للحملات ونوع المهمة والحالة، واختبار rollout يعيد `AUTOMATION_MIGRATION_REQUIRED` بدل خطأ خادمي غير معالج قبل تطبيق migration.
- بقي النشر والجدولة والإنفاق قرارات منفصلة؛ لا توسع هذه الدفعة صلاحية التنفيذ الخارجي.

نتيجة بوابة الجودة بعد الدفعة الثالثة:

- `npm run lint`: ناجح.
- `npm run type-check`: ناجح.
- الاختبارات: 3,151 ناجحة داخل 433 ملفًا، وملفا اختبار متخطيان بالكامل، و33 اختبارًا متخطى، بلا فشل.
- `npm run build`: ناجح، وتم توليد 136 مسارًا.
- عقد migrations: ناجح على PostgreSQL محلي معزول ونظيف، بما فيه RLS، عزل مساحات العمل، قيد مهمة نشطة واحدة، وحدود التقدم.
- فحص المتصفح المحلي بعد التنفيذ: Arabic وRTL من أول تحميل، 1280/1440 Desktop و390×844 Mobile بلا Horizontal Overflow، وقائمة الهاتف تفتح وتعرض كل الروابط وCTA بصورة سليمة. تم فحص Landing، Today، Campaigns، وApprovals بحساب محلي فعلي بلا Console errors.
- فحص AI حقيقي باستهلاك مصغر: وصل إلى OpenAI خلال 1.388 ثانية، لكن الاعتماد الحالي أعاد `429 rate_limited`؛ لذلك أعاد readiness حالة `503` صحيحة. الكود جاهز، لكن البيئة التشغيلية تحتاج رصيد OpenAI متاحًا أو اعتماد Vercel AI Gateway قبل اعتبار التوليد الحي جاهزًا.

### تطبيق قاعدة المشروع والاختبار الحي

تم تنفيذ بوابة ما قبل Staging على مشروع Supabase المتصل فعليًا:

- طُبقت migration الطابور باسم `20260729074931_durable_automation_jobs.sql`، ثم طُبقت migration فهرس الـFK المركب باسم `20260729075040_index_automation_job_campaign_workspace_fk.sql`. أرقام الملفات المحلية تطابق سجل Supabase لمنع migration drift.
- تم التحقق من وجود الجدولين والـenums وكل القيود والفهارس، وتفعيل RLS، وعدم امتلاك `anon` أو `authenticated` أي صلاحيات DML عليهما.
- كشف Performance Advisor أن الـFK المركب يحتاج فهرس `(campaignId, workspaceId)`؛ تمت إضافته في migration مستقلة. لم تظهر ثغرة جديدة بسبب الجداول. تحذير Auth السابق الخاص بتعطيل Leaked Password Protection ما زال قائمًا خارج نطاق هذه الدفعة.
- Health التفصيلي يثبت أن `automationJobsDatabase.state = ready` وأن قاعدة البيانات قابلة للوصول. الحالة الإجمالية ما زالت `503` فقط لأن مزود OpenAI يعيد `429 rate_limited`.
- تم تنفيذ طلب استراتيجية فعلي من حساب الاختبار كصاحب شركة: راجع المستخدم تكلفة 12 كريدت من رصيد تجربة ظاهر 15، وأكد الطلب، ووصل الاستدعاء إلى OpenAI بعد ثلاث محاولات. أعاد المزود `insufficient_quota`.
- فشل المزود لم ينشئ حملة أو Job ناقصًا ولم يثبت أي Debit؛ أعادت واجهة الرصيد 15 كريدت و0 توليدات. هذا يثبت أن مسار الفشل لا يفقد رصيد المستخدم.
- واجهة Automation المملوكة للمستخدم أعادت `200` و`job: null` بعد تطبيق الـschema بدل `AUTOMATION_MIGRATION_REQUIRED`.
- أُعيد فحص Landing وToday على 390×844: لا Horizontal Overflow، قائمة الهاتف تعمل، RTL صحيح، ولا Console errors. تم إلغاء كل جلسات حساب الاختبار بعد الانتهاء.
- أُعيد تشغيل `npm run verify` بعد التطبيق: Lint وTypeScript ناجحان، و3,151 اختبارًا ناجحًا بلا فشل، وProduction Build ناجح مع 136 صفحة.
- أُعيد عقد migrations من الصفر على PostgreSQL 17 مؤقت ومعزول، وشمل migration الفهرس الجديدة وانتهى بـ`marketing_migration_contract_passed`.

### الدفعة الرابعة — أمر واحد لصاحب الشركة

تم تقليل أول رحلة حملة من إدارة عدة وحدات إلى قرار أعمال واحد:

- أضيفت إلى Today نقطة بداية واضحة لصاحب الشركة: يختار فقط Leads أو Sales أو Awareness أو Website Traffic.
- ينشئ NEXUS مسودة حملة من Brand Brain ويستنتج الجمهور والنبرة والقنوات والاسم دون مطالبة المستخدم بفهم مصطلحات التسويق.
- عملية التحضير Idempotent ومحددة بالمستخدم؛ إعادة نفس الطلب أو تزامنه لا تنشئ حملات مكررة.
- لا تخصم عملية التحضير رصيدًا، ولا تمنح إذن نشر أو إنفاق. قبل تشغيل الاستراتيجية يرى المستخدم تكلفة 12 كريديت والرصيد قبل/بعد ويوافق صراحة.
- أصبحت متابعة مهمة الاستراتيجية دائمة عبر Refresh: تستعيد Today الـJob، تعرض التقدم، وتعيد إيقاظ المهمة القابلة للاستئناف.
- تم إصلاح مودال الرصيد على مستوى المنتج: أثناء جلب الرصيد لا يعرض ترقية زائفة، وفشل التحقق يمنع الخصم ويعرض رسالة «لم يُخصم شيء».
- تم اختبار التجربة بصريًا على 1440×900 و390×844 بالعربية والإنجليزية وRTL، بلا Horizontal Overflow أو Console errors، مع بقاء الإجراء النهائي ظاهرًا وقابلًا للوصول.
- نُفذ اختبار API حي بحساب Supabase مؤقت: الطلب الأول أعاد `201`، وإعادة نفس المفتاح أعادت `200` ونفس معرف الحملة، مع حملة واحدة فقط و0 Jobs و0 خصم و`publishAuthorized=false` و`spendAuthorized=false`.
- أُلغيت جلسات حساب الاختبار وحُذفت بياناته بعد التحقق، وتأكد عدم بقاء User أو Workspace أو Campaign أو Auth Session.

بوابة الجودة بعد الدفعة الرابعة:

- `npm run verify`: ناجح بالكامل.
- Lint وTypeScript: ناجحان.
- الاختبارات: 3,165 ناجحة داخل 437 ملفًا، وملفا اختبار متخطيان بالكامل و33 اختبارًا متخطى، بلا فشل.
- Production Build: ناجح، وتم توليد 136 صفحة.
- `git diff --check`: ناجح.

### الدفعة الخامسة — تسليم Server-side من الاستراتيجية إلى المحتوى

تم إغلاق أهم فجوة مباشرة بعد مراجعة الاستراتيجية دون توسيع صلاحيات المنتج:

- أصبحت موافقة صاحب الشركة على الاستراتيجية وتفويض تكلفة حزمة المحتوى **أمرًا واحدًا إلى الخادم** بدل طلب موافقة يعقبه طلب توليد منفصل من المتصفح.
- يتحقق الخادم من موافقة الكريديت والسعر الحالي ومفتاح Idempotency قبل تسجيل القرار؛ السعر القديم أو الطلب غير المحمي لا يتحول إلى موافقة جزئية قابلة للخصم.
- بعد تثبيت الاعتماد، يسلّم الخادم المهمة مباشرة إلى Workflow المحتوى الدائم بنفس مفتاح العملية، ويعيد `202 Accepted` مع رابط المهمة وحالة الاستكمال.
- إعادة نفس الأمر لا تعيد توليد حزمة موجودة ولا تخصم لها رصيدًا؛ يحتفظ النظام بالمسودات الحالية وتبقى إعادة التوليد قرارًا منفصلًا وصريحًا.
- إذا نجح اعتماد الاستراتيجية وفشل التسليم الداخلي أو مزود AI، يعيد العقد حقيقة جزئية واضحة: الاعتماد محفوظ، المحتوى يحتاج إعادة محاولة، ولا يوجد إذن نشر أو إنفاق.
- تم تعديل Today حتى لا يقفز صاحب الشركة إلى Connections عندما تكون لديه حملة بلا محتوى؛ يعيده الآن إلى قرار الاستراتيجية أو استكمال حزمة المحتوى بحسب الحالة.
- يترجم مسار الفشل رسائل مزود AI وبوابة Brand Brain إلى لغة أعمال عربية، ويصرح عند انطباق الحالة أن إنشاء المحتوى لم يبدأ وأن الكريديت لم يُخصم.

الاختبار الحي للدفعة الخامسة:

- عُرضت موافقة واحدة بالعربية توضح 6 كريديت، والمخرج «مسودات محتوى»، وأن النشر والإنفاق خارج التفويض.
- ضغط المستخدم الزر النهائي مرة واحدة؛ سجّل المتصفح طلبًا خارجيًا واحدًا فقط إلى Strategy Approval، بينما تم تسليم المحتوى داخل الخادم.
- حُفظت الاستراتيجية بحالة `ACTIVE` وSnapshot واحد، ثم أوقفت بوابة جودة Brand Brain بيانات الاختبار الاصطناعية قبل إنشاء المهمة.
- بقي الرصيد 30، وعدد التوليدات 0، وعدد Jobs والمنشورات وعمليات الخصم 0؛ ولم يُمنح إذن نشر أو إنفاق.
- فُحصت الشاشة على 390×844 بالعربية وRTL: لا Horizontal Overflow، المودال كامل داخل الشاشة، ولا Console errors.
- أُلغيت كل جلسات حساب QA ثم حُذفت بياناته، وتأكد عدم بقاء User أو Workspace أو Campaign أو Job أو Auth Session.

بوابة الجودة بعد الدفعة الخامسة:

- `npm run verify`: ناجح بالكامل.
- Lint وTypeScript: ناجحان.
- الاختبارات: 3,176 ناجحة داخل 439 ملفًا، وملفا اختبار متخطيان بالكامل و33 اختبارًا متخطى، بلا فشل.
- Production Build: ناجح، وتم توليد 136 صفحة.
- `git diff --check`: ناجح.

### الدفعة السادسة — اعتماد الحزمة وتسجيل الجدول الداخلي

تم تقليل آخر انتقال يدوي داخل Content Hub إلى قرار صاحب شركة واحد، مع إبقاء النشر والإنفاق خارج التفويض:

- أصبح المستخدم يراجع Copy والوسائط النهائية والمواعيد الدقيقة داخل نافذة واحدة، ثم يرسل أمرًا خارجيًا واحدًا إلى الخادم.
- ينفذ الخادم فحص ملكية الحملة، جاهزية كل وسيط، جودة المحتوى، اكتمال تاريخ مستقبلي لكل منشور، وثبات النسخ المراجعة قبل تسجيل أي قرار.
- يحفظ الأمر ثلاث طبقات إثبات مستقلة: `CONTENT_APPROVAL` و`CONTENT_MEDIA_APPROVAL` و`SCHEDULE_DECISION`.
- الجدول الناتج `MANUAL` وداخلي فقط؛ لا يربط حساب منصة، لا يرسل محتوى، لا يفعّل Autopilot، ولا يمنح إذن إنفاق.
- العملية لا تستهلك كريديت، وتدعم إعادة المحاولة دون تكرار جدول قائم. وإذا فشلت مرحلة بعد اعتماد سابق يعرض العقد الحالة الجزئية الحقيقية بدل ادعاء التراجع.
- أصبحت Today تعيد المستخدم إلى «مراجعة حزمة المحتوى وتسجيل الجدول» عندما تكون الحزمة جاهزة بلا Schedule، بدل دفعه مبكرًا إلى Connections.
- بقيت أدوات الاعتماد والجدولة المنفصلة متاحة فقط لاستكمال الحالات القديمة أو الجزئية، بينما أصبح المسار الأساسي للمستخدم العادي قرار حزمة واحدًا.

الاختبار الحي للدفعة السادسة:

- دخل مستخدم Supabase مؤقت إلى Content Hub بالعربية وراجع منشورين، وسائطهما النهائية، وتاريخي 31 يوليو و2 أغسطس 2026.
- عرضت النافذة تكلفة 0 كريديت وحدود «لا نشر خارجي، لا Autopilot، ولا صرف ميزانية» قبل القرار.
- ضغط المستخدم قرار الحزمة مرة واحدة؛ سجل الخادم طلبًا خارجيًا واحدًا إلى `approve-content-package`، ثم نفذ الاعتمادات الداخلية المحمية.
- تحوّل المنشوران إلى `SCHEDULED` بنمط `MANUAL`، مع ثلاث Snapshots جديدة وسجل انتقال كامل.
- بقيت `publishedAt` و`platformPostId` و`platformUrl` و`autoPublishConsentAt` و`publishAttemptedAt` و`integrationId` فارغة.
- بقي الرصيد 30، وعدد التوليدات 0، وعدد معاملات الكريديت 0.
- فُحصت النافذة على Desktop و390×844 Mobile بالعربية وRTL: لا Horizontal Overflow، المواعيد قابلة للتحرير، المودال قابل للتمرير حتى زر القرار، ولا Console errors.

بوابة الجودة بعد الدفعة السادسة:

- `npm run verify`: ناجح بالكامل.
- Lint وTypeScript: ناجحان.
- الاختبارات: 3,187 ناجحة داخل 441 ملفًا، وملفا اختبار متخطيان بالكامل و33 اختبارًا متخطى، بلا فشل.
- Production Build: ناجح، وتم توليد 136 صفحة.
- `git diff --check`: ناجح.

### الدفعة السابعة — أوديت بصري ومسارات وأزرار شامل

تم تنفيذ مراجعة استخدام فعلية بحساب Supabase مؤقت وبيانات مترابطة بدل الاكتفاء بقراءة الكود:

- جرى حصر 74 صفحة، و209 مسارات API، ونحو 638 زرًا و316 استخدامًا للروابط. فحص AST لم يجد زرًا نشطًا بلا `onClick` أو سلوك `submit`، ولم يجد `Link` أو رابطًا بلا `href` أو بقيمة فارغة/وهمية. الأزرار الثلاثة بلا handler كانت حالات عرض معطلة صراحة.
- فُحصت كل عائلات الصفحات المحمية والعامة، ومسارات Redirect القديمة، وصفحات الحملات والمحتوى وCRM وBrand وAnalytics وBilling وSettings وConnections وOnboarding. كل Redirect مختبر انتهى إلى وجهته المقصودة.
- فُحصت 20 شاشة حرجة على 390×844، والشاشات الأساسية على 1440×900، بالعربية وRTL مع تبديل حي إلى الإنجليزية وLTR. لم يظهر Horizontal Overflow أو Runtime/Console error، والقائمة المحمولة تفتح وتغلق مع `aria-hidden` و`inert` صحيحين.
- نُفذ المسار العام من Landing Page إلى Lead Form مع UTM وموافقة صريحة. حُفظ Lead واحد، وسُجل `PAGE_VIEW` و`CTA_CLICK` كإشارات عميل، و`FORM_SUBMITTED` كتأكيد خادم، دون ادعاء تواصل أو Conversion غير موثق.
- نُفذت موافقة Brand Learning حقيقية ومهمة CRM حقيقية، وتطابقت الواجهة مع قاعدة البيانات وسجل النشاط. كما اختُبرت فلاتر Content Hub وحالاتها الفارغة، وبوابة الدفع المعطلة حتى اكتمال تحقق الأسعار.
- أُصلح حد تشغيل Landing Page: أي Snapshot قديم أو تالف يعيد الآن عقد `503 LANDING_PAGE_SNAPSHOT_INVALID` وحالة آمنة للمستخدم بدل سقوط الرسم بخطأ 500.
- أُزيلت أرقام Learning الصفرية المؤقتة التي كانت تظهر قبل اكتمال الطلب، وأصبح المستخدم يرى Loader صادقًا ثم الأدلة الفعلية فقط.
- أُزيل وميض Onboarding للمستخدم العائد؛ لا تظهر له رسالة «لنبدأ لأول مرة» قبل معرفة حالة Workspace، ثم يُعاد إلى Today بسلاسة.
- أضيفت حالة فارغة صريحة عند استبعاد كل منشورات Content Hub بالفلاتر، مع زر «مسح الفلاتر»، بدل شاشة تبدو بلا بيانات.
- صُحح موضع مساعد NEXUS ليبقى بعيدًا عن محتوى الهاتف في RTL وعن الشريط الجانبي على Desktop.

حدود الاختبار الحي:

- لم يُنفذ نشر إلى منصة خارجية، أو إنفاق إعلاني، أو دفع Stripe، أو Reset/حذف إنتاجي. تمت مراجعة بواباتها ومساراتها وعقودها دون منح صلاحية أو صرف مال.
- صفحات الكيان العام غير الموجود تعرض حالة آمنة، لكن بعض مسارات App Router الديناميكية قد ترسل HTTP 200 بسبب بدء البث قبل استدعاء `notFound()`؛ تحتاج هذه النقطة إعادة تنظيم حدود الجلب/البث حتى يصبح Status Code نفسه 404.
- توجد تحذيرات ESLint قديمة غير مانعة في أجزاء Legacy، وتبقى Live E2E لمنصات OAuth والدفع والنشر مرتبطة بحسابات Sandbox حقيقية.

بوابة الجودة بعد الدفعة السابعة:

- `npm run verify`: ناجح بالكامل.
- Lint وTypeScript: ناجحان بلا أخطاء مانعة.
- الاختبارات: 3,193 ناجحة داخل 443 ملفًا، وملفا اختبار متخطيان بالكامل و33 اختبارًا متخطى، بلا فشل.
- Production Build: ناجح، وتم توليد 136 صفحة.
- الفحص النهائي للنسخة المبنية: عرض 390×844 و1440×900 بلا Overflow، ومساعد NEXUS لا يتقاطع مع المحتوى أو الشريط الجانبي.
- أُلغيت جلسات حساب QA المؤقت عالميًا، ثم حُذف حسابه وWorkspace وكل الكيانات التابعة، وتأكد أن User وAuth User وAuth Sessions وكل المعرفات المستخدمة في الأوديت أصبحت صفرًا.

### الدفعة الثامنة — إزالة عائق AI quota وإثبات التوليد الحي

تم حل أول عائق إنتاجي متبقٍ بدل الاكتفاء بتغيير رسالة الخطأ:

- ثبت أن مفتاح OpenAI المباشر المتصل بالمشروع يصل إلى المزود لكنه يعيد `insufficient_quota`، بينما Vercel يحقن `VERCEL_OIDC_TOKEN` تلقائيًا ويتيح AI Gateway للمشروع.
- أضيف محول مركزي يوجّه كل طلبات Chat Completions النصية القديمة إلى Vercel AI Gateway عندما تتوفر هوية Gateway، مع إبقاء OpenAI المباشر fallback للتطوير المحلي.
- توحدت مسارات الاستراتيجية والمحتوى وSentinel والاقتراحات وBrand Brain وChat وPaid Pack وAd Copy على نفس سياسة المهلة وRetry وCircuit Breaker.
- يطبع المحول أسماء موديلات Gateway الصحيحة ويضيف Model fallback قابلًا للضبط، ويحوّل طلبات JSON إلى عقد Prompt صارم لأن Gateway لا يقبل `response_format` في هذا المسار.
- أصبح Health Check يستخدم الحد الأدنى الصحيح البالغ 16 output tokens عند فحص Gateway، ويفرق بين `quota_exhausted` وبين Rate Limit مؤقت بدل تصنيف كل 429 كحالة واحدة.
- تقبل Production Readiness الآن واحدًا من `AI_GATEWAY_API_KEY` أو `VERCEL_OIDC_TOKEN` أو `OPENAI_API_KEY`. بقيت صورة المزود منفصلة: صور FAL أو OpenAI المباشر فقط، فلا يُعامل اعتماد نصي كأنه اعتماد صور.
- أُصلح عقد فشل ثلاثة مسارات اقتراح قديمة حتى تظل أخطاء المزود `502` مع استرداد الرصيد، بدل الانزلاق إلى `500` بعد إدخال المحول المركزي.

الاختبار الحي للدفعة الثامنة:

- أعاد فحص Gateway المصغر `200` بالموديل `openai/gpt-4.1-mini`، وأعاد Health التفصيلي `aiProvider.state = healthy` و`reachable = true` وCircuit مغلقًا.
- نفذ مستخدم Supabase مؤقت رحلة عضوية حقيقية لمدة 30 يومًا: أنشأ NEXUS استراتيجية كاملة باسم `Harbor Ledger Demo Drive`، واجتازت عقد الاستراتيجية بدرجة 100 بعد الإصلاح الآلي الآمن.
- خُصم للاستراتيجية 12 كريديت مرة واحدة، ثم نجحت مراجعة Sentinel وخصمت 3، ثم وافق المالك على تسليم المحتوى وحُجزت/سُويت 6 كريديت مرة واحدة.
- انتقلت مهمة المحتوى `QUEUED → RUNNING → WAITING_FOR_APPROVAL` وأنشأت 3 منشورات DRAFT: اثنان لـInstagram وواحد لـLinkedIn.
- يطابق دفتر الرصيد الحقيقة: بدأ المستخدم بـ100 وانتهى بـ79، بثلاث معاملات `SETTLED` فقط وبإجمالي 21 كريديت، وكل معاملة تحمل تكلفة المزود واستخدام الـtokens.
- بقيت `integrationId` و`platformPostId` و`platformUrl` و`publishedAt` و`approvedAt` و`autoPublishConsentAt` فارغة لكل المنشورات. كما بقي `publishAuthorized=false` و`spendAuthorized=false`.

بوابة الجودة بعد الدفعة الثامنة:

- Lint وTypeScript: ناجحان بلا أخطاء مانعة.
- الاختبارات: 3,200 ناجحة داخل 443 ملفًا، وملفا اختبار متخطيان بالكامل و33 اختبارًا متخطى، بلا فشل.
- Production Build: ناجح، وتم توليد 136 صفحة.
- `git diff --check`: ناجح.
- حالة Health الإجمالية ما زالت `503` بسبب بوابات Billing وLanding Pages المطلوبة وغير المكتملة في البيئة الحالية؛ مزود الذكاء نفسه لم يعد سبب الـ503.
- أُلغيت جلسات حساب QA عالميًا ثم حُذف المستخدم وWorkspace والحملة والمنشورات والـJob والمعاملات التجريبية، وتأكد أن كل العدادات وAuth Sessions أصبحت صفرًا.

### الدفعة التاسعة — تصحيح جاهزية الإنتاج دون ادعاء دفع وهمي

تم حل أول عائق بيئي متبقٍ بحسب حقيقة الاعتمادات، لا بملء قيم شكلية:

- كشف تدقيق متغيرات Production أن `NEXT_PUBLIC_BILLING_ENABLED=true` بينما مفاتيح Stripe والأسعار المطلوبة فارغة؛ أي أن البيئة كانت تطلب Billing لا يمكنها تشغيله.
- عُطلت بوابة Billing في Production إلى أن يُربط حساب Stripe Sandbox/Live وتُنشأ المنتجات والأسعار والـWebhook الحقيقي. لم تُنشأ مفاتيح وهمية، ولم يُنفذ Checkout أو اشتراك أو حركة مالية.
- وُلد مفتاح HMAC عشوائي بطول 32 بايت وحُفظ كمتغير Production مشفر وحساس باسم `CRO_EVENT_HASH_KEY` دون طباعته أو كتابته داخل المستودع.
- نُشرت نسخة Production جديدة بنجاح: deployment `dpl_DJg4CuBMuVWQ4b95BS3qsi6FSfPf` في حالة `READY` ومربوطة بـ`www.nexus-grow.com`.
- أعاد `/api/health` العام `200` و`ok: true`. يبقى التفصيل التشخيصي محميًا عمدًا بـ`CRON_SECRET` ولا تُنزّل قيمته الحساسة إلى الجهاز المحلي.
- نفذ مستخدم Supabase مؤقت طلبًا موثقًا إلى `/api/landing-pages/readiness`: أعاد `200` و`ready: true`، مع `runtimeConfigured: true` و`databaseState: ready` و`leadCrmState: ready` ووجود جداول الصفحات والمراجعات وأحداث التحويل.
- حُذفت جلسة المستخدم المؤقت وحسابه بعد الفحص، وتأكد أن `authUserExists=false`. كما حُذف سكربت الفحص المؤقت ولم تُكتب أي أسرار على القرص.

بوابة الجودة بعد الدفعة التاسعة:

- Remote Production Build: ناجح، وتم توليد 136 صفحة.
- الاختبارات المركزة لعقود Runtime وBilling وLanding readiness وHealth: 21 ناجحة داخل 4 ملفات، بلا فشل.
- فحص Runtime errors للنشر الجديد: لا توجد أخطاء مسجلة.
- Landing Pages جاهزة تشغيليًا على Production.
- Billing الآن **معطل بأمان وصادق مع حالة الاعتمادات**، لكنه ليس مكتملًا وظيفيًا. تفعيله يتطلب ربط Stripe حقيقيًا وإثبات Checkout وWebhook في Sandbox قبل تحويل العلم إلى `true`.

بوابة النشر الحالية:

1. ✅ تم تطبيق migration الطابور وفهرس تغطية الـFK على قاعدة Supabase المتصلة.
2. ✅ Health التفصيلي: `automationJobsDatabase.state = ready` و`aiProvider.state = healthy`.
3. ✅ تم تنفيذ توليد الاستراتيجية ومراجعة Sentinel ومسار `CAMPAIGN_APPROVAL_PACKAGE` حتى `WAITING_FOR_APPROVAL` عبر Gateway، مع تسوية الرصيد مرة واحدة.
4. ✅ تم إثبات أمان الرصيد في النجاح والفشل وعدم وجود نشر أو إنفاق غير معتمد.
5. ✅ أصبحت Landing Pages جاهزة في Runtime وقاعدة البيانات مع مفتاح HMAC حساس.
6. ⛔ المتبقي قبل 100 شركة Self-service: ربط Stripe وتفعيل Billing قانونيًا وتقنيًا، ثم إثبات OAuth والنشر والـReadback في حسابات Sandbox خارجية.

الدفعات التسع تعالج أساس الاعتمادية وتقدم Owner-first UX وOrchestration دائمة لأول رحلة. أصبحت حزمة Copy + Media Direction موحدة على مستوى التنفيذ والحالة، وأصبح تسليمها من موافقة الاستراتيجية ثم اعتمادها وجدولتها داخليًا مسارين آمنين موجهين لصاحب الشركة. كما أصبحت المسارات والأزرار والواجهات الحرجة مختبرة عبر الاستخدام الفعلي لا القراءة الساكنة فقط. ما زال الدفع الحقيقي والنشر الموثق إلى مزود خارجي والقياس والتعلّم مغلق الحلقة ضمن المراحل التالية أدناه.

## الحكم التنفيذي

NEXUS ليس مشروعًا تجريبيًا ضعيفًا. لديه أساس هندسي جيد، اختبارات واسعة، تصميم بصري Premium، وحرص ممتاز على عدم اختلاق نتائج أو نشر/صرف أموال دون موافقة. لكن المنتج الحالي أقرب إلى **نظام تشغيل لمسوّق محترف** منه إلى **موظف تسويق ذاتي لصاحب شركة لا يعرف التسويق**.

المشكلة الأساسية ليست الألوان أو الكروت أو جودة الواجهة. المشكلة أن المستخدم ما زال مطالبًا بفهم وإدارة دورة التسويق:

`Brand Brain → Strategy → Content → Execution → Analytics → Learning`

بينما التجربة المطلوبة يجب أن تكون:

`اختر نتيجة العمل → NEXUS يجهّز وينفذ → وافق على القرارات الحساسة → راقب النتيجة`

### التقييم بعد الدفعة التاسعة

| المحور | خط الأساس | الآن | الخلاصة |
|---|---:|---:|---|
| جودة التصميم البصري | 8.5/10 | 8.8/10 | هوية قوية ومتسقة، واختبار واسع على Desktop وMobile وRTL بلا Overflow أو أخطاء تشغيل |
| جودة الكود والاختبارات | 7.5/10 | 8.9/10 | توحدت طبقة AI واختُبرت عقود النجاح والفشل والرصيد؛ الملفات الضخمة وتحذيرات Legacy ما زالت دينًا تقنيًا |
| سهولة الاستخدام لغير المسوّق | 4/10 | 7.7/10 | أزيلت حالات التحميل والـOnboarding والفلاتر المضللة؛ ربط المنصة والنشر والنتائج ما زالت رحلة لاحقة |
| الأتمتة الحقيقية | 2.5/10 | 7.5/10 | ثبت حيًا التحضير والاستراتيجية والمراجعة والتسليم إلى مسودات؛ النشر والقياس لم يدخلا دورة مستقلة كاملة بعد |
| جاهزية الاعتمادية للإنتاج | 5.5/10 | 9.1/10 للمسارات المفعلة | التوليد والرصيد والـQueue وLanding مثبتة؛ Billing معطل بصدق حتى ربط Stripe، والمتبقي OAuth والنشر الخارجي |
| صدق المنتج وحماية المستخدم | 9/10 | 9.2/10 | أدلة العميل والخادم مفصولة، ولا يدّعي المنتج نشرًا أو أداءً أو تواصلًا غير مثبت |

**التقييم الإجمالي الحالي: 8.7/10.** ارتفعت الجاهزية التشغيلية دون رفع تقييم المنتج شكليًا، لأن Billing لم يصبح ميزة عاملة بعد. المنتج مناسب لبدء Cohort مراقب من 10–15 شركة Design Partners، لكنه لم يصل بعد إلى جاهزية 100 شركة Self-service: يلزم إثبات الدفع وOAuth والنشر الموثق والـReadback في Sandbox، وإغلاق دورة النتائج والتعلّم.

## نطاق المراجعة

تمت مراجعة:

- كل ملفات الصفحات البالغ عددها 74 صفحة، مع 20 مسار Redirect/Legacy تقريبًا.
- كل مسارات API البالغ عددها 209.
- فحص ساكن لنحو 638 زرًا و316 استخدامًا للروابط، مع تفاعل حي مع القرارات والفلاتر والقوائم والنماذج الآمنة.
- بنية Auth وi18n وAI والـAutopilot والتسعير والتعلّم.
- الصفحات الأساسية على الإنتاج بحساب فعلي: الصفحة العامة، Today، Approvals، Brand Brain، Strategy، Content Production، Execution Calendar، Analytics، Operations، Leads، Landing Pages، Connections، Billing، Settings، Campaigns، Media، Studio، Publishing Readiness، Learning، وPaid Campaigns.
- حالات Desktop وMobile وRTL، وفتح القائمة الجانبية والمودال وتبديل اللغة وFAQ والفلاتر وشاشات التكلفة.
- لم يتم تنفيذ الأزرار ذات الأثر الخارجي أو المدمر: فصل الحسابات، Reset، الدفع، النشر، صرف ميزانية، أو أي توليد يخصم كريديت. تمت مراجعة حالاتها ونصوصها ومساراتها دون إطلاق أثر حقيقي.

## نتائج التحقق الهندسي

- `npm run lint`: ناجح.
- `npm run type-check`: ناجح.
- الاختبارات عند خط الأساس: 3,084 ناجحة، 33 متخطاة، بلا فشل. بعد الدفعة الأولى: 3,122 ناجحة داخل 425 ملفًا. وبعد الدفعة الثانية: 3,143 ناجحة داخل 431 ملفًا. وبعد الدفعة الثالثة: 3,151 ناجحة داخل 433 ملفًا. وبعد الدفعة الرابعة: 3,165 ناجحة داخل 437 ملفًا. وبعد الدفعة الخامسة: 3,176 ناجحة داخل 439 ملفًا. وبعد الدفعة السادسة: 3,187 ناجحة داخل 441 ملفًا. وبعد الدفعة السابعة: 3,193 ناجحة داخل 443 ملفًا. وبعد الدفعة الثامنة: 3,200 ناجحة داخل 443 ملفًا، و33 متخطاة، بلا فشل. وبعد تعديل بيئة الدفعة التاسعة: 21 اختبار جاهزية مركزًا ناجحًا داخل 4 ملفات، بلا فشل.
- `npm run build`: ناجح، وتم توليد 136 مسارًا.
- `npm run db:verify-marketing-migrations`: ناجح على PostgreSQL مؤقت معزول، وانتهى بـ`marketing_migration_contract_passed`.
- Production Build يشغّل Lint وTypeScript الآن؛ لا توجد أخطاء مانعة، مع بقاء تحذيرات legacy كدين تقني منفصل.
- Live readiness: قاعدة البيانات والـAutomation جاهزتان، وVercel AI Gateway يعيد `200` وحالة `healthy`، وLanding readiness يعيد `200` و`ready: true`. أعيدت بوابة Billing إلى حالة disabled الصادقة بدل طلب تشغيلها دون مفاتيح Stripe.
- حجم المشروع: نحو 231,052 سطر TypeScript/TSX.
- قاعدة البيانات: Prisma schema بحجم 2,767 سطرًا و62 نموذجًا.
- لا توجد تغييرات سابقة غير مرتبطة بالمراجعة في Git وقت بدء العمل.

## أهم نقاط القوة

1. **الصدق التشغيلي ممتاز**

   الحالات تفرق بوضوح بين الجدولة الداخلية والنشر الحقيقي، وبين إشارات المتصفح والتأكيد من الخادم، وبين الملاحظة والسبب. هذا يقلل خطر الادعاءات الكاذبة ويصلح كقاعدة Enterprise قوية.

2. **بوابات الموافقة والتكلفة جيدة**

   رحلة الاستراتيجية تعرض التكلفة والرصيد قبل الخصم، وتوضح ما هو داخل النطاق وما هو خارجه. لا يبدأ الخصم إلا في التأكيد النهائي.

3. **أساس التعلّم مسؤول**

   التعلّم لا يعدّل Brand Brain بصمت، ويحتاج أدلة فعلية وعينة كافية وموافقة وإمكانية تراجع. هذه قاعدة صحيحة لبناء Agent يتطور بأمان.

4. **الواجهة متماسكة**

   الألوان، التباين، البطاقات، العناوين، حالات الفراغ، والقائمة الجانبية متناسقة. لم يظهر Horizontal Overflow في الصفحات التي تم فحصها على Desktop أو Mobile.

5. **Mobile وRTL جيدان**

   الصفحة العامة ولوحة Today تعملان على عرض 390×844 دون كسر. القائمة تتحول إلى Drawer، تمنع Scroll خلفها، وتنقل Focus إلى زر الإغلاق.

## أهم المشكلات مرتبة حسب الخطورة

### P0 — وعد “المستخدم يوافق فقط” وصل إلى حزمة المحتوى وما زال ناقصًا في التنفيذ والنتائج

كان مسار Autopilot الأصلي يفعّل مراقبة Queue فقط، ولا يولد نصًا أو يجدول منشورًا أو يستدعي AI. الدفعة الرابعة أضافت مسار Owner Command حقيقيًا من Today:

- المستخدم يختار نتيجة العمل فقط.
- NEXUS ينشئ الحملة من Brand Brain.
- المستخدم يوافق على تكلفة الاستراتيجية.
- المهمة تعمل وتُستعاد تلقائيًا بعد مغادرة الصفحة أو تحديثها.

الدفعة الخامسة دمجت موافقة الاستراتيجية وتسليم حزمة المحتوى في أمر خادمي واحد. الرحلة الكاملة ما زالت تحتاج:

- مراجعة حزمة المحتوى في شاشة واحدة.
- موافقة مستقلة على الجدولة والنشر الخارجي.
- إعادة النتائج والتوصية التالية إلى Today.

**الأثر المتبقي:** البداية أصبحت مناسبة لصاحب الشركة، لكن NEXUS لم يصبح بعد موظفًا يغلق الدورة من الموافقة إلى التنفيذ والنتائج مع تدخل استثنائي فقط.

**القرار المطلوب التالي:** بعد اعتماد Content Approval Package، يبني Orchestrator اقتراح الجدولة ويفحص Connections آليًا، ثم يعرض موافقة واحدة للجدولة/النشر ويعيد النتائج إلى Today. تبقى الميزانية والنشر الخارجي والادعاءات الحساسة وراء موافقات صريحة.

### P0 عند خط الأساس — نقطة فشل واحدة في مزود الذكاء (عولجت في الدفعة الأولى)

كان الاستدعاء الرئيسي:

- يستخدم موديلًا واحدًا ثابتًا.
- يستخدم Fetch مباشرًا بلا Timeout.
- لا يحتوي Retry/Backoff واضحًا لأخطاء 429 و5xx.
- لا يحتوي Fallback model/provider.
- يرمي خطأ المزود مباشرة عند الفشل.

الدليل:

- `src/lib/ai/openai.ts:10`
- `src/lib/ai/openai.ts:24`

**الأثر:** رحلة استراتيجية طويلة ومكلفة يمكن أن تفشل بسبب Rate Limit أو انقطاع مؤقت، وهو أمر غير مقبول عندما يكون الذكاء هو الموظف الأساسي.

**الحالة الآن:** تم توحيد مهلة الطلب وإعادة المحاولة وBackoff وJitter والأخطاء المنظمة وCircuit Breaker، مع Gateway/Fallback في مسار الاستراتيجية وفحص صحة حي للمزوّد. يبقى تحويل كل عمليات التوليد الطويلة إلى Jobs قابلة للاستئناف ضمن العمل التالي.

### P0 عند خط الأساس — حماية الصفحات مشتتة (عولجت في الدفعة الأولى)

كان يوجد Middleware في جذر المشروع يحمي قائمة محدودة من المسارات عبر وجود Cookies قديمة فقط:

- `middleware.ts:4`

ويوجد Middleware آخر داخل `src` يصرح أن الحماية Client-side ويسمح بكل شيء تقريبًا:

- `src/middleware.ts:16`

مسارات مهمة مثل Approvals وOperations وLeads وLanding Pages وConnections ليست ضمن قائمة الحماية في Middleware الجذري.

**الأثر:** سلوك دخول غير موحد، احتمالية Flash/Loading غير ضروري، وصعوبة إثبات أن كل شاشة خاصة محمية Server-side.

**الحالة الآن:** يوجد Middleware واحد يعتمد Supabase SSR و`getClaims()`، وسياسة مركزية تفصل المسارات العامة عن كل صفحات مساحة العمل، مع Migration للجلسات القديمة.

### P1 — Onboarding يجمع معلومات ولا يصنع أول نتيجة

الـOnboarding مكوّن من خمس خطوات، ويصرح صراحة أنه لا يولد استراتيجية أو ينشر أو يصرف أو يؤتمت:

- `src/app/onboarding/page.tsx:3`
- `src/app/onboarding/page.tsx:78`

لا يبدأ من Website URL رغم وجود مسار لمسح الموقع في النظام.

**الأثر:** أول لقاء مع المنتج يطلب من المستخدم تعليم النظام، ثم يرسله إلى مرحلة أخرى، بدل أن يثبت قيمة المنتج خلال دقائق.

### P1 — التجربة المجانية لا تصل إلى أول محتوى

التجربة المجانية تعطي 15 كريديت وتستبعد Content Production عمدًا:

- `src/lib/commercialPlans.ts:11`

**الأثر:** المستخدم يتعلم كيف يعمل المنتج لكنه لا يحصل على نتيجة قابلة للاستخدام. هذا يقلل Activation ويجعل قرار الدفع مبنيًا على وعد لا تجربة.

### P1 — الواجهة تخاطب النظام والخبير أكثر من صاحب الشركة

أمثلة ظهرت في الصفحات:

- `First-party`
- `CLIENT_REPORTED`
- `SERVER_CONFIRMED`
- `MANUAL_CONFIRMED`
- `WON`
- `SANDBOX`
- `Pilot`
- `OAuth`
- `PlatformPostId`
- “تغطية دورة التشغيل”
- “قرارات الجدولة الموثقة”

القائمة الأساسية تعرض 11 وجهة:

- `src/components/Sidebar.tsx:301`

**الأثر:** المستخدم العادي يرى معمارية قسم التسويق بدل أن يرى عمله وقراراته ونتائجه.

### P1 — التعقيد التقني يبطئ التطوير

أكبر الملفات:

| الملف | السطور |
|---|---:|
| `campaigns/[id]/content-hub/page.tsx` | 7,022 |
| `campaigns/[id]/page.tsx` | 6,656 |
| `lib/i18n-context.tsx` | 4,466 |
| `brand/page.tsx` | 3,376 |
| `campaigns/[id]/creative-brief/page.tsx` | 3,144 |
| `paid-campaigns/[id]/page.tsx` | 2,008 |
| `RunFullStrategyModal.tsx` | 1,857 |
| `strategy/page.tsx` | 1,766 |

يوجد أكثر من 1,100 استخدام لـ`any`/`as any` في `src`.

**الأثر:** تعديل رحلة أساسية سيصبح بطيئًا وعالي المخاطر، حتى مع وجود اختبارات.

### P1 — نظام اللغة مزدوج

`Providers` يحتوي `I18nProvider`، ثم `RootLayout` يضيف `LanguageProvider` آخر:

- `src/app/providers.tsx:21`
- `src/app/layout.tsx:47`
- `src/contexts/LanguageContext.tsx:19`
- `src/lib/i18n-context.tsx:50`

كلاهما يبدأ بالإنجليزية ثم يستعيد Local Storage في Effect، بينما `<html lang="en">` ثابت وقت الـSSR.

**الأثر:** احتمالية Flash واختلاف Hydration وتعقيد غير ضروري. كما أن قاموس الترجمة نفسه 4,466 سطرًا ويستخدم `any`.

## الأوديت البصري وتجربة الاستخدام

### الصفحة العامة

**جيد:**

- Hero قوي جدًا، وهوية Premium واضحة.
- العربية والإنجليزية تعملان.
- Mobile ممتاز.
- FAQ يعمل.
- CTA واضح.

**يحتاج تغييرًا:**

- الوعد “قسم تسويق يعمل باستمرار” أقوى من التنفيذ الحالي.
- الصفحة طويلة وتشرح النظام كمنصة Enterprise.
- تبيع تفاصيل مثل traceability وapproval gates قبل أن تبيع نتيجة العمل.
- يجب أن يصبح الوعد: “اختر هدفك، وافق على الخطة، وNEXUS يدير التنفيذ والمتابعة”.

### Today

**جيد:**

- أفضل شاشة لبدء التجربة.
- تعرض قرارًا واحدًا.
- Mobile card ممتاز بصريًا.

**يحتاج تغييرًا:**

- بعد القرار الأول تظهر مقاييس تشغيل تقنية كثيرة.
- 20% و1/5 وسجلات التنفيذ دقيقة لكنها ليست النتيجة التي يريدها صاحب الشركة.
- يجب أن تعرض: “ماذا أنجز NEXUS؟ ماذا يحتاج موافقتك؟ وما النتيجة؟”.

### Approvals

**جيد:**

- نظيفة جدًا ومباشرة.
- حالة الفراغ صادقة.

**يحتاج تغييرًا:**

- يجب أن تصبح الشاشة المركزية للمنتج كله، لا رابطًا ثانويًا.
- القرار يحتاج Preview وسبب التوصية والأثر المتوقع والميزانية والمخاطر في بطاقة واحدة.

### Brand Brain

**جيد:**

- ناضجة بصريًا.
- تظهر التغطية والمصدر وحدود الحفظ التلقائي.

**يحتاج تغييرًا:**

- “مراجعة الملف، سجل النمو، اقتراحات المصادر، تحسين Brand Brain” أربعة مفاهيم مبكرة.
- للمستخدم العادي يكفي: “وجدنا هذه المعلومات عن شركتك — صحح الخطأ فقط”.

### Strategy

**جيد:**

- مودال أربع خطوات واضح.
- التكلفة والرصيد والنطاق والاستثناءات ممتازة.

**يحتاج تغييرًا جذريًا:**

- يطلب من المستخدم اختيار Organic/Paid/Full، 30/90/180 يومًا، عمق التخطيط، واللغة.
- هذه قرارات يجب أن يتخذها AI من الهدف والميزانية والسوق ثم يقدم توصية واحدة قابلة للتعديل.
- الاستراتيجية الحالية منتج مستقل، ثم المحتوى منتج مستقل، ثم الوسائط منتج مستقل؛ وهذا يعاكس “قسم كامل”.

### Content Production وMedia وStudio

**جيد:**

- فصل النص والوسائط والحقوق والجودة منظم.
- حالات الفراغ صادقة.

**يحتاج تغييرًا:**

- المستخدم يتنقل بين Content Hub وMedia وStudio.
- يجب دمجها في Approval Package واحد: Copy + Visual + Channel + Date.
- الأدوات المتخصصة تبقى في Advanced mode فقط.

### Execution وPublishing Readiness وConnections

**جيد:**

- فصل الجدولة عن النشر حقيقي ومهم.
- حالات الاتصال والصلاحية شفافة.

**يحتاج تغييرًا:**

- صفحة Connections تعرض تفاصيل تقنية أكثر مما يحتمله المستخدم العادي.
- يكفي عرض “جاهز / يحتاج إعادة ربط / غير مدعوم بعد”، ونقل OAuth/scopes/readback إلى Advanced diagnostics.
- يجب أن يقترح NEXUS القنوات والجدول، ثم يطلب موافقة واحدة على الحزمة.

### Analytics وLearning وOperations

**جيد:**

- هندسيًا من أقوى أجزاء المنتج.
- التعلّم قائم على دليل وليس ادعاء.

**يحتاج تغييرًا:**

- هي أكثر الصفحات تعقيدًا للمستخدم العادي.
- Operations يجب ألا تكون في التنقل الأساسي.
- Analytics يجب أن تبدأ بثلاثة أسئلة: ماذا حدث؟ لماذا نعتقد أنه حدث؟ ماذا سيفعل NEXUS بعد ذلك؟
- التفاصيل الإثباتية تبقى قابلة للتوسيع للخبير.

### Leads وLanding Pages

**جيد:**

- مسار First-party منطقي.
- لا يدعي إرسال رسائل أو تحويلات غير حقيقية.

**يحتاج تغييرًا:**

- الصفحات موجودة كأدوات منفصلة قبل أن يكون المسار الآلي كاملًا.
- يجب أن ينشئ NEXUS Landing Page draft وLead Form كجزء من Campaign Package بدل أن يطلب من المستخدم بناءهما يدويًا.

### Billing

**جيد:**

- صادقة بشأن عدم تفعيل الدفع.
- تعرض تكلفة كل عملية.

**يحتاج تغييرًا:**

- الصفحة طويلة ومعقدة وتحوّل المستخدم إلى محاسب كريديت.
- الباقات يجب أن تباع بنتائج/سعة تشغيل: “حملة كاملة شهريًا”، “3 حملات”، “نشر ومتابعة”.
- احتفظ بالكريديت داخليًا للحماية والتكلفة، ولا تجعله النموذج العقلي الأساسي للعميل.

## المنتج المستهدف

### تجربة المستخدم الجديدة

1. يدخل المستخدم Website URL أو Instagram account.
2. NEXUS يمسح الموقع والمحتوى ويجهز Brand Profile تلقائيًا.
3. المستخدم يجيب عن خمسة أسئلة فقط:
   - ما النتيجة المطلوبة؟
   - ما العرض الأساسي؟
   - ما السوق؟
   - ما الميزانية؟
   - ما الذي يمنع NEXUS من قوله أو فعله؟
4. NEXUS ينشئ Starter Campaign Package:
   - توصية استراتيجية واحدة.
   - رسائل وHooks وCTA.
   - 3–7 منشورات كاملة.
   - اتجاه بصري.
   - Landing Page draft عند الحاجة.
   - جدول نشر.
   - خطة قياس.
5. المستخدم يراجع شاشة واحدة ويختار:
   - وافق.
   - عدّل.
   - ارفض.
6. NEXUS يجدول وينفذ ما تسمح به الاتصالات والسياسة.
7. NEXUS يرسل Weekly Brief:
   - ما تم.
   - النتائج.
   - ما سيتغير الأسبوع القادم.
8. المستخدم يوافق على مقترح التعلّم، ثم تتكرر الدورة.

### التنقل الجديد

التنقل الافتراضي:

- **اليوم**
- **قراراتي**
- **النتائج**
- **الإعدادات والربط**

تحت “عرض متقدم”:

- Brand Brain
- Strategies
- Campaigns
- Content Studio
- Calendar
- Leads
- Landing Pages
- Operations
- Learning evidence

## خريطة الطريق المقترحة

### المرحلة 0 — تثبيت عقد المنتج (3 أيام)

المخرجات:

- تعريف North Star: “المستخدم يختار الهدف ويوافق؛ NEXUS يخطط وينتج وينفذ ويتعلم”.
- تحديد الأفعال التي تحتاج موافقة:
  - نشر خارجي.
  - صرف/تغيير ميزانية.
  - ادعاء غير مثبت.
  - تعديل Brand Brain.
  - تغيير كبير في الخطة.
- كل ما عدا ذلك يصبح تلقائيًا وقابلًا للتراجع.

معيار الانتهاء:

- وثيقة واحدة للحالات والسياسات.
- لا توجد شاشة أو Feature جديدة خارج هذا العقد.

### المرحلة 1 — الاعتمادية أولًا (أسبوعان)

الأعمال:

1. توحيد Auth في Server-side app layout ومصدر Middleware واحد.
2. استخدام تحقق Supabase server session بدل وجود Cookie فقط.
3. حماية كل Route داخل `(app)` وإبقاء `(public)` منفصلًا.
4. بناء AI Provider Gateway:
   - Timeout.
   - Retry مع Exponential Backoff وJitter.
   - معالجة 429/5xx.
   - Fallback model.
   - Circuit breaker.
   - Usage/cost tracing.
5. تحويل التوليد الطويل إلى Jobs قابلة للاستئناف مع Idempotency.
6. توحيد i18n وإزالة Provider القديم ونقل القواميس إلى ملفات Locale typed.
7. جعل Lint جزءًا إلزاميًا من Build بدل `ignoreDuringBuilds`.
8. Health check فعلي لمزود الذكاء، لا مجرد وجود المفتاح.

حالة التنفيذ الحالية:

- مكتمل: 1، 2، 3، 5 لمساري Campaign Engine وContent Approval Package، 7، 8.
- مكتمل للمسارات الأساسية وجارٍ تعميمه: 4.
- جزئي: 6؛ تم توحيد Provider والـSSR locale، وما زال تقسيم القاموس الكبير إلى ملفات typed.
- متبقٍ قبل إغلاق المرحلة بالكامل: تعميم Jobs الدائمة على توليد الوسائط المدفوعة وباقي العمليات الطويلة، ثم تقسيم القاموس الكبير إلى ملفات Locale typed.

معيار الانتهاء:

- نجاح رحلة AI ≥98% بعد إعادة المحاولة.
- لا خصم مزدوج.
- لا Route خاص يظهر دون Session مؤكدة.
- فشل Job لا يفقد العمل ويمكن استكماله.

### المرحلة 2 — Owner-first UX (3 أسابيع)

الأعمال:

1. تقليل التنقل الافتراضي إلى أربع وجهات.
2. إعادة بناء Today حول:
   - “NEXUS يعمل الآن على…”
   - “يحتاج موافقتك”
   - “أنجز هذا الأسبوع”
   - “النتيجة”
3. بناء Unified Approval Card.
4. إعادة كتابة النصوص بلغة الأعمال وإخفاء المصطلحات التقنية.
5. URL-first onboarding.
6. تقديم AI recommendation واحدة بدل مصفوفة اختيارات Expert.
7. إضافة Advanced mode للخبراء بدل إجبار الجميع عليه.

معيار الانتهاء:

- مستخدم بلا خبرة يفهم الخطوة التالية في أقل من 10 ثوان.
- لا يحتاج معرفة Organic/Paid/Depth أو First-party/OAuth.
- لا يزيد الإدخال اليدوي الأولي عن 7 حقول/قرارات.

### المرحلة 3 — Autopilot حقيقي (4 أسابيع)

ابنِ Orchestrator بحالات واضحة:

`DISCOVER → PLAN → PRODUCE → QUALITY_CHECK → NEEDS_APPROVAL → SCHEDULE → PUBLISH → MEASURE → PROPOSE_LEARNING`

المكونات:

- Work item/Job model.
- Step attempts وRetry policy.
- Approval policy engine.
- Budget guard.
- Idempotency keys.
- Audit trail.
- Resume from failure.
- Human override وPause.
- Notification عند الاستثناء فقط.

أول Workflow كامل:

- Campaign goal.
- Strategy.
- Content plan.
- 3 منشورات كاملة.
- Media direction.
- Landing page draft عند هدف Leads.
- Approval package.
- Schedule proposal.

معيار الانتهاء:

- من Website URL إلى Approval Package واحد دون تنقل يدوي بين الوحدات.
- المستخدم لا ينشئ Strategy وContent Plan وMedia كلًا على حدة.
- كل خطوة قابلة للتتبع والتراجع.

### المرحلة 4 — التنفيذ والقياس (4 أسابيع)

الأعمال:

- ربط Workflow بالمنصات الجاهزة فعليًا.
- Token refresh ومراقبة الصلاحية تلقائيًا.
- Preflight واحد بدل عدة صفحات.
- نشر مع Provider ID وReadback.
- UTM وLanding Page وLead capture تلقائيًا.
- Weekly Results Brief.
- استثناءات النشر تظهر في Approvals/Decisions.

معيار الانتهاء:

- حملة واحدة تغلق الحلقة من الخطة إلى نشر موثق وLead/metric موثق.
- لا يحتاج المستخدم دخول صفحة Connections إلا عند عطل أو ربط أول مرة.

### المرحلة 5 — التعلّم المغلق الحلقة (3 أسابيع)

استفد من الأساس الحالي بدل استبداله:

- Approval preference learning.
- Performance evidence thresholds.
- Proposals مع مصدر وثقة.
- Apply/Rollback.
- Controlled experiments.

معيار الانتهاء:

- كل تغيير يشرح: الدليل، مستوى الثقة، الأثر المقترح، وإمكانية التراجع.
- لا يوجد “AI learned” دون بيانات وموافقة.

### المرحلة 6 — تفكيك التعقيد بالتوازي

الأعمال:

- تقسيم الصفحات الأكبر حسب Feature boundaries.
- نقل Data fetching وmutations إلى hooks/services typed.
- توحيد Auth helper في API routes.
- إزالة Prisma `as any`.
- توليد API contracts typed.
- استبدال `<img>` الحرجة بـ`next/image`.
- وضع Budget للحزم:
  - Dashboard أقل من 180 kB First Load.
  - Campaign detail أقل من 250 kB.
- اختبارات بصرية حقيقية Screenshot/interaction بدل اختبارات Strings فقط.

## تعديل التسعير والتجربة

### التجربة المجانية الجديدة

بدل 15 كريديت لا تصل إلى المحتوى:

- Website/brand scan.
- Starter Brand Profile.
- خطة 14 يومًا.
- 3 منشورات كاملة بصور/اتجاه بصري منخفض التكلفة.
- Approval Package.
- Schedule preview.
- صفحة نتائج تجريبية تشرح ما سيقاس.

هذا يعطي العميل دورة صغيرة كاملة، ويمنع الإسراف أفضل من إعطائه نصف رحلة.

### الباقات

اعرض الباقات بوحدة نتيجة:

- Growth: حملة تشغيل كاملة واحدة شهريًا.
- Autopilot: حتى 3 حملات نشطة مع نشر ومراقبة وتعلّم.

يبقى Ledger الكريديت داخليًا وشفافًا في صفحة التفاصيل، لكنه لا يكون الرسالة الرئيسية.

## خطة الوصول إلى 100 شركة

لا تبدأ بـ100 شركة دفعة واحدة. استخدم Cohorts مع بوابات جودة:

### Cohort 1 — 10 شركات Design Partners

- قطاعات متقاربة.
- جلسة Setup مراقبة.
- قياس كل Click وكل تدخل يدوي.
- لا توسع قبل نجاح 8 من 10 في إنشاء Approval Package خلال 10 دقائق.

### Cohort 2 — 25 شركة

- Self-serve onboarding.
- دعم داخل المنتج.
- قياس أسباب الرفض والفشل.
- لا توسع قبل نجاح Workflow الكامل ≥90%.

### Cohort 3 — 50 شركة

- تفعيل الدفع الحقيقي.
- مراقبة تكلفة AI وهوامش كل Workflow.
- Weekly retention ووقت المستخدم.
- لا توسع قبل Gross Margin مستهدف وSupport load مقبول.

### Cohort 4 — 100 شركة

- قوالب حسب القطاع.
- Referral.
- Case studies مبنية على نتائج موثقة.
- Playbook onboarding ودعم واضح.

## مؤشرات النجاح

| المؤشر | الهدف |
|---|---:|
| Time to first Approval Package | ≤10 دقائق |
| عدد مدخلات المستخدم في البداية | ≤7 |
| إكمال أول دورة صغيرة في التجربة | ≥70% |
| نجاح Jobs بعد Retry | ≥98% |
| قبول المسودة الأولى أو تعديل بسيط | ≥70% |
| الوقت اليدوي الأسبوعي للمستخدم | ≤20 دقيقة |
| تفعيل أول منصة | ≥60% من المستخدمين النشطين |
| احتفاظ الأسبوع الرابع في Pilot | ≥40% |
| حملات أغلقت حلقة نشر→قياس→تعلم | مقياس رئيسي، لا Vanity metric |
| أخطاء خصم مزدوج أو نشر بلا موافقة | صفر |

## ترتيب التنفيذ النهائي

1. لا تضف صفحات جديدة الآن.
2. أصلح Auth وAI reliability وJobs.
3. غيّر الـOnboarding والتجربة المجانية لتسليم نتيجة كاملة.
4. بسّط التنقل إلى Today / Decisions / Results.
5. ابنِ Orchestrator الحقيقي.
6. أغلق حلقة النشر والقياس والتعلّم.
7. ابدأ Cohort من 10 شركات، ثم 25، ثم 50، ثم 100.

القاعدة الحاكمة: **إذا كانت الخطوة يستطيع NEXUS استنتاجها بأمان، فلا تطلبها من المستخدم. وإذا كانت الخطوة حساسة، اعرض توصية واحدة واطلب موافقة واضحة.**
