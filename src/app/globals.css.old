@tailwind base;
@tailwind components;
@tailwind utilities;

/* ─── Base reset ────────────────────────────────────────────────────── */

* { margin: 0; padding: 0; box-sizing: border-box; }

html { scroll-behavior: smooth; }

body {
  font-family: var(--font-geist-sans), -apple-system, BlinkMacSystemFont,
    'Segoe UI', 'Helvetica Neue', sans-serif;
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
  background: #080807;
  color: #F5F0E8;
}

/* ─── Motion keyframes ──────────────────────────────────────────────── */

@keyframes fadeIn {
  from { opacity: 0; }
  to   { opacity: 1; }
}

@keyframes slideUp {
  from { transform: translateY(10px); opacity: 0; }
  to   { transform: translateY(0);    opacity: 1; }
}

@keyframes slideDown {
  from { transform: translateY(-8px); opacity: 0; }
  to   { transform: translateY(0);    opacity: 1; }
}

@keyframes shimmer {
  0%   { background-position: -600px 0; }
  100% { background-position:  600px 0; }
}

@keyframes pulse {
  0%, 100% { opacity: 1;   }
  50%       { opacity: 0.4; }
}

@keyframes ambientPulse {
  0%, 100% { opacity: 0.3; }
  50%       { opacity: 0.7; }
}

@keyframes glowPulse {
  0%, 100% { box-shadow: 0 0 14px rgba(255,149,0,0.12); }
  50%       { box-shadow: 0 0 32px rgba(255,149,0,0.28); }
}

@keyframes float {
  0%, 100% { transform: translateY(0px); }
  50%       { transform: translateY(-5px); }
}

@keyframes aiScan {
  0%   { opacity: 0; transform: translateY(-4px); }
  20%  { opacity: 1; }
  80%  { opacity: 1; }
  100% { opacity: 0; transform: translateY(4px); }
}

/* ─── Surface system ────────────────────────────────────────────────── */

.surface-card {
  background: #131312;
  border: 1px solid #1f1f1d;
  box-shadow:
    inset 0 1px 0 rgba(255,255,255,0.03),
    0 1px 3px rgba(0,0,0,0.6),
    0 4px 16px rgba(0,0,0,0.28);
  border-radius: 12px;
  transition: border-color 0.15s ease, box-shadow 0.15s ease;
}
.surface-card:hover {
  border-color: #2a2a26;
  box-shadow:
    inset 0 1px 0 rgba(255,255,255,0.05),
    0 2px 8px rgba(0,0,0,0.55),
    0 12px 32px rgba(0,0,0,0.32);
}

.surface-panel {
  background: #0d0d0c;
  border: 1px solid #1f1f1d;
  box-shadow:
    inset 0 1px 0 rgba(255,255,255,0.04),
    0 4px 24px rgba(0,0,0,0.5),
    0 24px 64px rgba(0,0,0,0.32);
  border-radius: 16px;
}

.surface-inset {
  background: #0a0a09;
  border: 1px solid #191918;
  box-shadow: inset 0 2px 8px rgba(0,0,0,0.35);
  border-radius: 10px;
}

.surface-accent {
  background: rgba(255, 149, 0, 0.07);
  border: 1px solid rgba(255, 149, 0, 0.18);
  box-shadow:
    inset 0 1px 0 rgba(255,149,0,0.10),
    0 0 20px rgba(255,149,0,0.06);
  border-radius: 12px;
}

/* ─── Atmospheric backgrounds ───────────────────────────────────────── */

.bg-atmospheric {
  background:
    radial-gradient(ellipse 55% 35% at 10% 65%, rgba(255,149,0,0.035) 0%, transparent 60%),
    radial-gradient(ellipse 35% 25% at 90% 10%, rgba(255,149,0,0.025) 0%, transparent 55%),
    #080807;
}

.bg-sidebar {
  background: linear-gradient(180deg, #0d0d0c 0%, #080807 100%);
  border-right: 1px solid #191918;
}

.bg-hero {
  background:
    radial-gradient(ellipse 80% 55% at 50% 0%, rgba(255,149,0,0.07) 0%, transparent 65%),
    radial-gradient(ellipse 40% 30% at 80% 85%, rgba(255,149,0,0.03) 0%, transparent 50%),
    #080807;
}

/* ─── Skeleton shimmer ──────────────────────────────────────────────── */

.skeleton {
  background: linear-gradient(
    90deg,
    #131312 25%,
    #1f1f1d 50%,
    #131312 75%
  );
  background-size: 800px 100%;
  animation: shimmer 1.8s infinite linear;
  border-radius: 7px;
}

/* ─── Page transitions ──────────────────────────────────────────────── */

.page-enter {
  animation: slideUp 0.28s cubic-bezier(0.22, 1, 0.36, 1) both;
}

.stagger > * {
  animation: slideUp 0.24s cubic-bezier(0.22, 1, 0.36, 1) both;
}
.stagger > *:nth-child(1) { animation-delay: 0ms; }
.stagger > *:nth-child(2) { animation-delay: 50ms; }
.stagger > *:nth-child(3) { animation-delay: 100ms; }
.stagger > *:nth-child(4) { animation-delay: 150ms; }
.stagger > *:nth-child(5) { animation-delay: 200ms; }
.stagger > *:nth-child(6) { animation-delay: 250ms; }

/* ─── AI presence utilities ─────────────────────────────────────────── */

.ai-pulse {
  position: relative;
}
.ai-pulse::before {
  content: '';
  position: absolute;
  inset: -1px;
  border-radius: inherit;
  background: linear-gradient(135deg, rgba(255,149,0,0.07), transparent);
  pointer-events: none;
  animation: ambientPulse 3.5s ease-in-out infinite;
}

.live-dot {
  position: relative;
  display: inline-flex;
  width: 8px;
  height: 8px;
}
.live-dot::before {
  content: '';
  position: absolute;
  inset: 0;
  border-radius: 50%;
  background: #4ade80;
  animation: pulse 2s cubic-bezier(0.4, 0, 0.6, 1) infinite;
  opacity: 0.6;
  transform: scale(1.8);
}
.live-dot::after {
  content: '';
  position: absolute;
  inset: 0;
  border-radius: 50%;
  background: #4ade80;
}

/* ─── Typography utilities ──────────────────────────────────────────── */

.text-display {
  font-size: clamp(36px, 5vw, 60px);
  font-weight: 700;
  line-height: 1.02;
  letter-spacing: -0.035em;
  color: #F5F0E8;
}

.text-headline {
  font-size: clamp(24px, 3vw, 36px);
  font-weight: 700;
  line-height: 1.1;
  letter-spacing: -0.025em;
  color: #F5F0E8;
}

.text-label {
  font-size: 10px;
  font-weight: 700;
  letter-spacing: 0.12em;
  text-transform: uppercase;
  color: #5C5448;
}

/* Gradient text — Nexus Ember signature */
.gradient-text {
  background: linear-gradient(135deg, #FF9500 0%, #FFB340 55%, #FFD080 100%);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
}

/* Shimmer text effect */
.shimmer {
  background: linear-gradient(90deg, #f8fafc 0%, #fbbf24 25%, #f59e0b 50%, #fbbf24 75%, #f8fafc 100%);
  background-size: 200% auto;
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
  animation: shimmer 3s linear infinite;
}

/* ─── Nav utilities ─────────────────────────────────────────────────── */

.nav-blur {
  background: rgba(8, 8, 7, 0.88);
  backdrop-filter: blur(20px) saturate(150%);
  -webkit-backdrop-filter: blur(20px) saturate(150%);
  border-bottom: 1px solid rgba(255,255,255,0.04);
}

/* ─── Interaction utilities ─────────────────────────────────────────── */

.transition-smooth {
  transition: all 0.18s cubic-bezier(0.22, 1, 0.36, 1);
}

button {
  cursor: pointer;
  transition: all 0.15s cubic-bezier(0.22, 1, 0.36, 1);
  font-family: inherit;
}
button:disabled { cursor: not-allowed; opacity: 0.4; }

a { color: inherit; text-decoration: none; }

/* ─── Scrollbar ─────────────────────────────────────────────────────── */

::-webkit-scrollbar          { width: 5px; height: 5px; }
::-webkit-scrollbar-track    { background: transparent; }
::-webkit-scrollbar-thumb    { background: rgba(255,149,0,0.22); border-radius: 4px; }
::-webkit-scrollbar-thumb:hover { background: rgba(255,149,0,0.42); }

/* ─── Selection ─────────────────────────────────────────────────────── */

::selection {
  background: rgba(255, 149, 0, 0.25);
  color: #fff;
}

/* ─── Focus ─────────────────────────────────────────────────────────── */

input:focus, textarea:focus, select:focus {
  outline: none;
  border-color: rgba(255,149,0,0.55);
  box-shadow: 0 0 0 3px rgba(255,149,0,0.10);
}

/* ─── Glass ─────────────────────────────────────────────────────────── */

.glass {
  background: rgba(8, 8, 7, 0.72);
  backdrop-filter: blur(16px) saturate(140%);
  border: 1px solid rgba(255,255,255,0.05);
}

/* ─── Ember glow — signature Nexus effect ───────────────────────────── */

.ember-glow {
  box-shadow: 0 0 32px rgba(255,149,0,0.18), 0 0 64px rgba(255,149,0,0.07);
}

.ember-border {
  border: 1px solid rgba(255,149,0,0.28);
}

/* ─── Utility ───────────────────────────────────────────────────────── */

.tabular-nums { font-variant-numeric: tabular-nums; }
